## Release v1.0.1 Lessons
- Git push restrictions on `main` branch require manual PR creation from a release branch.
- Tags should be pushed explicitly after PR merge to avoid confusion.
- Manual upload to Chrome Web Store is preferred when automated credentials are not configured.
